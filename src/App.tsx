export default function App() {
  return <div style={{ fontFamily: "var(--body)", padding: 40, background: "var(--paper)", minHeight: "100vh", color: "var(--ink)" }}>Emir One v3 — scaffold OK</div>;
}
